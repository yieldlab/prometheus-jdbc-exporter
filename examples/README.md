# Usage examples for prometheus-jdbc-exporter

Currently, there's one usage example on how to use prometheus-jdbc-exporter from
within [Docker Compose] inside the [`docker-compose`](docker-compose) folder.
